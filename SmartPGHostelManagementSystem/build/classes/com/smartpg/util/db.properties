# Smart PG Hostel Management System - database connection settings
#
# Loaded by DBConnection.java from the classpath (this file sits next to
# DBConnection.java and Eclipse copies it into build/classes automatically,
# the same way it already copies the compiled .class files - no .classpath
# change needed).
#
# These are the SAME connection values the project used before this file
# existed; only their location changed, out of DBConnection.java's source
# code and into this config file. Edit the values below for your local
# MySQL setup instead of editing DBConnection.java.

db.url=jdbc:mysql://localhost:3306/smartpg
db.username=root
db.password=subhadeep
